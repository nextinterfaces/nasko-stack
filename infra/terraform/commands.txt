ssh-keygen -t ed25519 -C "hetzner-k3s" -f ~/.ssh/id_ed25519_hetzner

export HCLOUD_TOKEN # TODO place yout Hetzner Api Token here"

export TF_VAR_ssh_public_key_path=~/.ssh/id_ed25519_hetzner.pub

echo $HCLOUD_TOKEN
echo $TF_VAR_ssh_public_key_path

terraform init -upgrade
terraform plan
terraform apply -auto-approve

# This runs on your Mac after apply
terraform plan -detailed-exitcode
# expect exit code 0 (no changes). 2 means there are changes; nonzero in CI = fail
